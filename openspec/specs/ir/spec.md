# Spec: ir (source of truth)

> Populated when `define-ir-contract` is archived. Until then, the contract lives
> in the change at `openspec/changes/define-ir-contract/` and in
> `docs/IR-CONTRACT.md`. Do not hand-edit this file; it is produced by archiving
> approved changes.
