{
  "_comment": "INVALID: edge 'to' references a non-existent resource id. expect_error_contains: alpha.alpha_token.ghost",
  "_expect_error_contains": "alpha.alpha_token.ghost",
  "schemaVersion": 1,
  "providers": { "alpha": { "source": "./bin/provider-alpha", "config": {} } },
  "resources": [
    { "id": "alpha.alpha_token.A", "provider": "alpha", "type": "alpha_token", "name": "A", "config": {} }
  ],
  "edges": [
    { "from": "alpha.alpha_token.A", "to": "alpha.alpha_token.ghost", "via": "label" }
  ],
  "nixConsumers": []
}
