{
  "_comment": "A datasource read whose output feeds a resource: the canonical datasource shape. The aws_instance references the datasource's id via a __ref, producing an edge from the datasource to the resource.",
  "schemaVersion": 1,
  "providers": {
    "aws": { "source": "./bin/provider-aws", "config": {} }
  },
  "resources": [
    {
      "id": "aws.aws_instance.web",
      "provider": "aws",
      "type": "aws_instance",
      "name": "web",
      "config": {
        "ami": { "__ref": { "resource": "data.aws.aws_ami.ubuntu", "path": ["id"] } },
        "instance_type": "t3.micro"
      }
    }
  ],
  "dataSources": [
    {
      "id": "data.aws.aws_ami.ubuntu",
      "provider": "aws",
      "type": "aws_ami",
      "name": "ubuntu",
      "config": { "most_recent": true }
    }
  ],
  "edges": [
    { "from": "data.aws.aws_ami.ubuntu", "to": "aws.aws_instance.web", "via": "ami" }
  ],
  "nixConsumers": []
}
